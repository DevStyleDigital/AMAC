import styled from 'styled-components';

export const WhatsappFixed = styled.a`
  width: 50px;
  height: 50px;
  position: fixed;
  z-index: 4;
  border-radius: 50%;
  right: 1rem;
  bottom: 1rem;
  background-color: #FFF;
  box-shadow: 0 0 5px rgba(0,0,0, 0.6);
  img{
    width:100%;

  }
`;
